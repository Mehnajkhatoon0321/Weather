import 'package:flutter/material.dart';
class AppColors extends MaterialColor {
  const AppColors(int primary, Map<int, Color> swatch) : super(primary, swatch);
  static const primaryColorCyan = Color(0xFF34C1D1);

}